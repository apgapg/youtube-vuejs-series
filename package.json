{
  "private": true,
  "scripts": {
    "deploy": "slidev build --base /youtube-vuejs-series/ && gh-pages -d dist",
    "build": "slidev build",
    "dev": "slidev --open",
    "export": "slidev export"
  },
  "dependencies": {
    "@slidev/cli": "^0.28.2",
    "@slidev/theme-default": "*",
    "@slidev/theme-seriph": "*"
  },
  "name": "vuejs-series",
  "devDependencies": {
    "gh-pages": "^3.2.3"
  }
}
